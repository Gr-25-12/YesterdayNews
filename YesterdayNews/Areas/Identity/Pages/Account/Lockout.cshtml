@page
@model LockoutModel
@{
    ViewData["Title"] = "Locked Out!";
}

<header class="text-center mt-5">
    <h1 class="text-danger fw-bold">
        🔒 @ViewData["Title"]
    </h1>

    <p class="text-muted fs-5">
        Well… either your account has been <strong>locked by an admin</strong>
        or you decided to <strong>vanish into thin air</strong> 🫠.
    </p>

    <p class="text-secondary">
        Either way, you can’t get in right now.
        But hey, no hard feelings — maybe it’s time for a new adventure! 🚀
    </p>

    <div class="mt-4">
        <p class="text-muted">
            Don’t worry, we’ll escort you out politely...
        </p>

        <div class="mt-4">
            <p class="fw-bold">
                Launching the catapult in <span id="countdown" class="text-primary fs-3">10</span> seconds 🚀
            </p>
            <div id="countdown-message" class="text-secondary mt-2"></div>

            <p class="mt-3 text-muted small fw-bold">
                💡 You can contact us to know the issue...
                if you manage to find our contact details. if you managed to reach to this text 😅 😏
            </p>
        </div>
    </div>
</header>

@section Scripts {
    <script>
        let seconds = 10;
        const countdownEl = document.getElementById('countdown');

        const timer = setInterval(() => {
            seconds--;
            countdownEl.textContent = seconds;

            if (seconds <= 0) {
                clearInterval(timer);
                window.location.href = "/"; 
            }
        }, 1000);
    </script>
}
